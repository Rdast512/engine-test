#include <stdio.h>

using namespace std;


int main() {
    // This is a dummy function to demonstrate the structure
    // of a C++ file with a main function.
    int x = 10;
    int y = 20;
    int sum = x + y;
wada
    // Output the result
    printf("The sum of %d and %d is %d\n", x, y, sum);
}