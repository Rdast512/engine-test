#include "vk_scene.hpp"

void Scene::setBaseAxes(const glm::vec3& /*right*/, const glm::vec3& /*upDir*/, const glm::vec3& /*forward*/) noexcept
{
    // Reserved for scene orientation; no state stored yet beyond startPosition.
}
